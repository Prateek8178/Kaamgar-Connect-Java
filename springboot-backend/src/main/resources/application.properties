server.port=${PORT:8080}

# MongoDB Atlas
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://Prateek1123:Prateek1306_@cluster0.n9th04w.mongodb.net/kaamgar?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.auto-index-creation=true

# JWT
jwt.secret=${JWT_SECRET:kaamgar-connect-jwt-secret-change-in-production-2025}
jwt.expiration=604800000

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USER:sahupratik93@gmail.com}
spring.mail.password=${EMAIL_PASS:bgng unpe ifuh exrv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS
app.cors.allowed-origins=${CLIENT_URL:http://localhost:3000}

# Upload directory
app.upload.dir=uploads

# Logging
logging.level.com.kaamgar=INFO
